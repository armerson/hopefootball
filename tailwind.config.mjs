/** @type {import('tailwindcss').Config} */
const config = {
  theme: {
    extend: {
      colors: {
        primary: "#0B1F3A",
        accent: "#2563EB",
      },
    },
  },
};

export default config;
